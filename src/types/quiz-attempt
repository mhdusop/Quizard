import { Quiz } from "./quiz";
import { Answer } from "./answer";

export interface QuizAttempt {
   id: string;
   startedAt: Date;
   endedAt?: Date;
   score?: number;
   quizId: string;
   userId: string;
   completed: boolean;
   quiz?: Quiz;
   answers?: Answer[];
}
